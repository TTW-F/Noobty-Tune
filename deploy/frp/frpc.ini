# ============================================================
# frpc 客户端参考模板 —— 与内网机 192.168.31.35 实际部署一致
# 实际位置:/home/qwe/frp_0.52.3_linux_amd64/frpc.ini
# (frpc v0.52.3,INI 格式;该文件被多个服务共用,
#  追加 section 前先备份,重启 frpc 会瞬断所有隧道)
# ============================================================

[common]
server_addr = 106.14.175.93
server_port = 7000
token = CHANGE_ME_KEEP_IN_SYNC_WITH_FRPS

# ---- Noobty Tune 后端(本项目) ----
[noobty-tune-backend]
type = tcp
local_ip = 127.0.0.1
local_port = 3101
remote_port = 3101
