# ============================================================
# frpc 客户端配置 —— 运行于内网后端机 192.168.31.35
#
# 职责:把本机 127.0.0.1:3101(noobty-tune 后端)穿透到
# 云服务器 106.14.175.93 的 127.0.0.1:3101。
#
# 上线前必改:auth.token 换成强随机串(与 frps.toml 保持一致)
# ============================================================

serverAddr = "106.14.175.93"
serverPort = 7500

auth.token = "CHANGE_ME_TO_A_STRONG_RANDOM_TOKEN"

[[proxies]]
name = "noobty-tune-backend-3101"
type = "tcp"
localIP = "127.0.0.1"
localPort = 3101
remotePort = 3101
